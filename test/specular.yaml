packages:
  - origin: "file://package.specular"
    version: 0.0.1
    path:
      module: testpackage
      namespace: specularjs
    generate:
      - language: js
        client:
          output: ./generated-client
          options:
            _skipJS: true
            specularRuntimeImport: "../.."
